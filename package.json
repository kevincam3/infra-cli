{
  "name": "@kevincam3/infra-cli",
  "version": "1.0.0",
  "private": true,
  "description": "CLI for orchestrating per-project Docker Compose stacks (infrastructure, applications, tooling) with environment-aware secret export.",
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kevincam/infra-cli.git"
  },
  "bin": {
    "infra": "./bin/infra.sh"
  },
  "files": [
    "bin",
    "lib",
    "examples",
    "scripts",
    "README.md"
  ],
  "scripts": {
    "postinstall": "node scripts/postinstall.mjs",
    "prepare": "vp config"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.2",
    "@commitlint/config-conventional": "^20.5.0",
    "vite-plus": "^0.1.19"
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  },
  "engines": {
    "node": ">=22"
  }
}
